// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // User site (repo named Alejandroanzac.github.io), so it is served at the root
  // with no base path.
  site: 'https://alejandroanzac.github.io',
});
