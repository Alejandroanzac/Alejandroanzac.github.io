// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // User site (repo named Alejandroanzac.github.io) → served at the root, no base path.
  site: 'https://alejandroanzac.github.io',
});
